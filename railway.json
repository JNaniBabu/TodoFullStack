{
  "deploy": {
    "releaseCommand": "python manage.py migrate",
    "startCommand": "python manage.py collectstatic --noinput && python manage.py create_initial_superuser && gunicorn Backend.wsgi --bind 0.0.0.0:$PORT"
  }
}
