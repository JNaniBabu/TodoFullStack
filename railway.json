{
  "deploy": {
    "releaseCommand": "python manage.py migrate && python manage.py collectstatic --noinput && python manage.py create_initial_superuser",
    "startCommand": "gunicorn Backend.wsgi --bind 0.0.0.0:$PORT"
  }
}